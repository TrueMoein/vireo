# Vireo development environment template.
# Copy to `.env` and fill in real values. `.env` is gitignored.
#
# Get an OpenRouter API key at https://openrouter.ai/keys
# Recommended models are ≥30B params (see docs/llm-providers.md).

OPENROUTER_API_KEY=
